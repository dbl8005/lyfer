import 'package:flutter/material.dart';

/// UI constants used throughout the app for consistent styling
class UIConstants {
  // Loading indicators
  static const double defaultLoadingSize = 24.0;
  static const String loadingHabitsLabel = 'Loading habits';

  // Error handling
  static const String errorPrefix = 'Failed to load habits: ';

  // Semantics labels
  static const String habitsScreenLabel = 'Habits screen';

  // Animation durations
  static const Duration defaultTransitionDuration = Duration(milliseconds: 300);
}
